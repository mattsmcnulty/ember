/** Must match config.schema.json pluginAlias and the config.json "platform" value. */
export const PLATFORM_NAME = 'Ember';
/** Must match the npm package name. */
export const PLUGIN_NAME = 'homebridge-ember';
